const ردود_الكرة_الثمانية = [
    "نعم، بالتأكيد!",
    "مستحيل!",
    "اسأل مرة أخرى لاحقاً.",
    "من المؤكد.",
    "مشكوك فيه للغاية.",
    "بدون شك.",
    "ردي هو لا.",
    "العلامات تشير إلى نعم."
];

async function أمر_الكرة_الثمانية(سوك, معرف_الدردشة, سؤال) {
    if (!سؤال) {
        await سوك.sendMessage(معرف_الدردشة, { text:  يرجى طرح سؤال!  });
        return;
    }

    const رد_عشوائي = ردود_الكرة_الثمانية[Math.floor(Math.random() * ردود_الكرة_الثمانية.length)];
    await سوك.sendMessage(معرف_الدردشة, { text: `🎱 ${رد_عشوائي}` });
}

module.exports = { أمر_الكرة_الثمانية };